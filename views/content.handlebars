<section class="container">
    <h1 class="user-name">{{username_email}}</h1>
    <h2 class="post-title">{{title}}</h2>
    <section class="flex-row justify-center">
        {{#each post.content as |content|}}
        {{> content-details}}
        {{/each}}
    </section>
</section>